// Multiples of Six
// Print multiples of 6 up to 60,000, using a WHILE.

// function sixMult()
var i = 1;
var mult = [];

while (i < 60001) {
  temp = 6 * i;
  mult.push(temp);
  i++;
  }
console.log (mult);
//}
